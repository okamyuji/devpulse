# 改善バックログ

シールを妨げない改善候補を次版のために記録します。

## agent-sessions-basic-design v1.0 のシール時点の残件

| 種別 | 内容 | 出所 |
|---|---|---|
| 未検証事項 | Claude CodeのCLIが返すstate値の全値域は未網羅（実測ではfailedとblockedのみ観測）。詳細設計での状態対応表は観測済みの値＋未知値の受け皿で構成する必要がある | 1巡目R2の未検証申告 |
| 未検証事項 | cmux eventsのイベント種別の網羅は未検証（実測ではagent.hook.SubagentStopとfeed.item.receivedを観測） | 1巡目R2の未検証申告 |
| 表記統一の余地 | 3.1節の取得元名（詳述形）と7.3節の表の行見出し（短縮形）の表記差。3巡目R3が「誤読は構成できず許容範囲」と判定したため未修正 | 3巡目R3 |

## 処理済みの記録

1巡目の指摘9件、2巡目の指摘3件は、すべて実在確認（実行コマンドと出力、または文書行引用）を伴った上でv0.2およびv0.3で修正済みです。バックログへ送った未対応の指摘はありません。

次の6件は、agent-sessions-detailed-design v1.1（2026-07-19）で文書へ反映済みです。実装第1段と手動検証からのフィードバック5件（v1.1候補として記録していたもの）と、v1.0シール時点の残件1件です。

| 種別 | 内容 | 出所 | 反映先 |
|---|---|---|---|
| 設計前提の訂正 | 4.1節の「events --limit指定の単発読み取り」は、保持イベント残数がlimit未満のときliveイベント待ちでブロックすることが実装時の実測で判明（--limit 30で2分タイムアウト）。単発取得の意図はタイムアウト時の部分出力回収で維持する | 実装第1段の実測 | v1.1の4.1節 |
| 設計解釈の明文化 | 未統合のcmux surface（素のシェル等）の扱い: ttyがpsの端末集合に生きていてエージェントプロセスが無いsurfaceは行にせず、ttyが存在しないsurfaceのみorphaned行とする解釈を採用 | 実装第1段の設計判断 | v1.1の4.1節 |
| 内部属性の追記 | 統一行モデルにactivity_source（直列化されない内部属性）を追加 | 実装第1段の設計判断 | v1.1の2節 |
| 設計前提の訂正 | 4.1節のevents紐付け「session_idまたはcwdが一致する行」のうちcwd照合は誤帰属を起こすため廃止（手動検証で他セッションの活動が付く実測。イベントのcwdはフック時の作業ディレクトリでセッション一意でない）。session_id照合のみに変更し、取得もカーソルファイル方式（--cursor-file、cmux再起動時はackフレームでstale検出しリセット）へ変更 | 手動検証で発見した欠陥の修正 | v1.1の4.1節 |
| 内部属性の追記 | 統一行モデルにsession_id（直列化されない内部属性）を追加（events照合用） | 同上 | v1.1の2節 |
| テスト計画の追記 | 8.4節のスナップショット鮮度表示（2周期閾値の判定と収集停止表示）に対応するテスト行が10節に無い | 最終確認レビューの単独Low | v1.1の10節 |
